"""Parser package: timing-report and Liberty-library parsing helpers.

Exposed for import as `parsers.timing_rpt_parser` / `parsers.library_extractor`
by the orchestrator (src/main_orch.py) and context builder.
"""
